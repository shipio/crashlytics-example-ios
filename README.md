Crashlytics IO Example

This iOS app uses the Crashlytics library to register crashes with the team@ship.io Crashlytics account.

It is primarily used for testing that Crashlytics OTA works with Ship.io.
